const Navbar = () => {
  return (
    <nav className="nav-bar">
      <div id="logo"></div>
      <a href="#">About</a>
      <a href="#">Experience</a>
      <a href="#">Contact</a>
    </nav>
  );
};

export default Navbar;
